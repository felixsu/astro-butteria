---
export function getStaticPaths() {
  return [
    { params: { locale: 'en' } },
    { params: { locale: 'id-ID' } },
  ];
}

const { locale } = Astro.params;
return Astro.redirect(`/${locale}/home`);
---
