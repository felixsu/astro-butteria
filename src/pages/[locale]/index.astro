---
import { supportedLocales } from '../../config';
export function getStaticPaths() {
  return supportedLocales.map((locale) => ({
    params: { locale },
    props: { locale },
  }));
}
const { locale } = Astro.props;
Astro.response.status = 302;
Astro.response.headers.set('Location', `/${locale}/home`);
---
